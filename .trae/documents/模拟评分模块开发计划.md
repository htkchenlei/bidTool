# 模拟评分模块开发计划

## Summary

用户请求开发模拟评分模块的 9 个核心功能子模块。经代码库探索确认：**所有 9 个模块均已在前一轮会话中实现完成**，后端核心函数、前端状态变量、HTML 视图、CSS 样式全部就位。本计划聚焦于"验证 + 收尾"，不再重复实现。

## Current State Analysis（基于 Phase 1 探索）

### 后端实现状态（[backend/routes/bid_score.py](file:///d:/Python/bidToolByWorkbuddy1/backend/routes/bid_score.py)）

| 模块 | 对应函数/实现 | 行号 | 状态 |
|------|--------------|------|------|
| 1 文档接入与解析 | `run_bid_score` 调用 `parse_file` + `extract_text_segments`，支持 DOCX/PDF/IMAGE | 1210-1354 | ✅ 完成 |
| 2 招标规则结构化 | `_extract_scoring_criteria` + `_build_criteria_extraction_messages` + `_normalize_criteria` | 267-308 | ✅ 完成 |
| 3 投标信息抽取 | `_extract_bid_info`（报价/分项/商务证据/技术章节） | 311-436 | ✅ 完成 |
| 4 价格评分引擎 | `_calculate_price_scores`（最低价/基准价/线性插值，小微企业扣除，最高限价） | 439-551 | ✅ 完成 |
| 5 商务证据识别与计分 | `_score_commercial_evidence` + `_match_evidence_to_criterion`（规则去重） | 554-641 | ✅ 完成 |
| 6 技术主观评分引擎 | `_score_single_bid`（仅 technical 类，sub_scores/reason/evidence/confidence） | 644-929 | ✅ 完成 |
| 7 分数汇总与排名 | `_generate_bid_score_stream` 阶段7，同分报价低者优先 `sorted(key=lambda r: (-total_score, adjusted_price))` | 1065-1153 | ✅ 完成 |
| 8 可解释报告与审计 | `audit` 字段 + `_build_score_report_docx`（含审计章节） | 1162-1198, 1374+ | ✅ 完成 |
| 9 主流程编排 | `_generate_bid_score_stream` 分 8 阶段（parsing→criteria→extracting→price→commercial→technical→ranking→complete） | 930-1206 | ✅ 完成 |

### 前端实现状态

- **[frontend/static/js/app.js](file:///d:/Python/bidToolByWorkbuddy1/frontend/static/js/app.js)**
  - `BID_SCORE_ALLOWED_EXTS = ['.docx', '.pdf', '.png', '.jpg', '.jpeg']`（行 1658）✅
  - `bidScoreProgress = ref({ stage, message, progress })`（行 1660）✅
  - `BID_SCORE_STAGES`（7 阶段定义，行 1662）✅
  - `addBidScoreFiles` 白名单校验（行 1688）✅
  - `runBidScore` 重置 + 更新进度（行 1754, 1806）✅
  - `downloadBidScoreReport` 携带 audit（行 2219 暴露）✅

- **[frontend/templates/index.html](file:///d:/Python/bidToolByWorkbuddy1/frontend/templates/index.html)**
  - 进度条：`.bid-score-progress-wrap/bar/bar-fill/percent`（行 949-953）✅
  - 阶段指示器：`.bid-score-stages`（行 955-958）✅
  - 审计卡片：`.bid-score-audit-card/audit-grid/audit-item`（行 1116-1144）✅

- **[frontend/static/css/main.css](file:///d:/Python/bidToolByWorkbuddy1/frontend/static/css/main.css)**
  - 进度条样式（行 4468-4500）✅
  - 阶段指示器样式（行 4500+）✅
  - 分类标签 `.bid-score-category-tag--price/--commercial/--technical`（行 4542-4567）✅
  - 证据引用 `.bid-score-evidence`（行 4568）✅
  - 置信度 `.bid-score-confidence/confidence-bar`（行 4579-4597）✅
  - 审计卡片样式（行 4619-4654）✅

## Assumptions & Decisions

1. **实现已完成**：前一轮会话（上下文已丢失）已完成全部 9 模块的代码实现，本次计划不重写代码。
2. **遵循硬约束**：结果不持久化（仅内存审计，随响应返回 + 写入 docx）、Claude 不传 temperature、模型配置通过参数传递、模型失败返回可用模型列表。
3. **架构为混合方案**：价格分纯规则引擎、商务分 LLM 抽取+规则去重、技术分 LLM 主观评分。
4. **同分处理**：`sorted(key=lambda r: (-total_score, adjusted_price))`，报价低者优先。

## Proposed Changes（收尾工作）

由于核心实现已完成，仅需以下收尾步骤：

### Step 1：最终验证（只读）
- 运行 `python -m py_compile backend/routes/bid_score.py backend/llm_client.py backend/doc_parser.py` 确认无语法错误
- 调用 `GetDiagnostics` 检查 `frontend/static/js/app.js` 与 `frontend/templates/index.html` 无诊断错误

### Step 2：更新项目记忆
更新 [c:/Users/96888/.trae-cn/memory/projects/-d-Python-bidToolByWorkbuddy1/project_memory.md](file:///c:/Users/96888/.trae-cn/memory/projects/-d-Python-bidToolByWorkbuddy1/project_memory.md)，记录本次新增的工程约定：
- 模拟评分 9 模块分阶段架构（价格分规则引擎 / 商务分 LLM 抽取+规则去重 / 技术分 LLM 主观评分）
- 新增后端函数签名：`_extract_bid_info`、`_calculate_price_scores`、`_score_commercial_evidence`、`_match_evidence_to_criterion`，均支持 `model_config` 参数
- 新增前端样式类约定：`.bid-score-progress-*`、`.bid-score-stage*`、`.bid-score-category-tag--{price|commercial|technical}`、`.bid-score-evidence`、`.bid-score-confidence`、`.bid-score-audit-card`
- 同分处理规则：`sorted(key=lambda r: (-total_score, adjusted_price))`，报价低者优先
- 价格评分三种方法：`lowest`（最低价法）/ `base_price`（基准价法）/ `linear`（线性插值法）
- DEFAULT_CRITERIA 含 `category`/`calc_method`/`sub_items` 字段；DEFAULT_PRICE_RULE 常量

### Step 3：向用户报告完成
返回最终完成响应，说明 9 个模块已实现并通过验证。

## Verification Steps

1. `python -m py_compile` 三个后端文件全部 exit code 0
2. `GetDiagnostics` 对 app.js 和 index.html 返回无错误
3. 项目记忆文件已追加新约定

## Out of Scope

- 不重新实现任何已完成的代码
- 不新增超出 9 模块范围的功能
- 不修改硬约束（结果不持久化等）
- 不做运行时端到端测试（需用户手动验证）
